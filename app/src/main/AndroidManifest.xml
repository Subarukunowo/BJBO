<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <meta-data
        android:name="osmdroid.cache"
        android:value="CACHE_DIR" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BJBO"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">>


        <activity android:name=".LoginActivity"
            android:exported="true"/>
        <activity android:name=".BerandaActivity"
            android:exported="false"/>
<activity android:name=".RegisterActivity"/>
        <activity android:name=".ExploreActivity" />

        <activity android:name=".JualActivity"/>
        <activity android:name=".PilihLokasiActivity"/>
        <activity android:name=".PembayaranActivity"/>
<activity android:name=".ProfileActivity"/>
        <activity android:name=".AccountActivity"/>
        <activity android:name=".DetailPostinganActivity" />

    </application>

</manifest>